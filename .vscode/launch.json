{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "lldb debug",
      "program": "${command:cmake.launchTargetPath}",
      "args": ["output/output.bin", "output/output.bin"],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false
    }
  ]
}
